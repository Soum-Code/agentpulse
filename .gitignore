# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# Node
node_modules/
dashboard/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local Claude Code tool config (not part of the project)
.claude/skills/

# Environment
.env
*.env.local

# Database
*.db
*.db-shm
*.db-wal
*.sqlite3
data/

# Models
models/
*.onnx
*.bin

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Testing
.coverage
htmlcov/
.pytest_cache/

# Logs
*.log
logs/
omniroute_claude_code_guide.md

# External derived data — regenerable from experiments/external_exgentic_ingest.py
# at the pinned revision in datasets/external/exgentic_v2/source_metadata.json.
# Provenance (source_metadata.json, raw/manifest.json, README.md) stays tracked.
datasets/external/*/derived/

# Embedding cache — regenerable, large, derived from gitignored external data
experiments/results/.drift_embedding_cache.npz

# Agent/plugin skill caches — tooling, not project source
.agents/

# Nested checkout with its own .git — tracking it would create a broken
# gitlink rather than real content. Make it a submodule if it is meant to
# ship with the project.
agenttrace/

# Personal profile scrape, not project source. The repo is private today,
# but this would follow it if it were ever made public.
linkedin_profile.md

# Office lock files
~$*
